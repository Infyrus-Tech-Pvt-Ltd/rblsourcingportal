services:
  rbl-portal:
    image: ghcr.io/rkjha-infyrus/rblsourcingportal:latest
    container_name: rbl-portal
    ports:
      - "8090:8090"
      - "5050:5050"
    volumes:
      - ./backend:/app/backend
    env_file:
      - .env
    restart: unless-stopped